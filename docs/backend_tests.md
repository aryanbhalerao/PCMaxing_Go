# Backend Tests

| Test Name | Description | Status |
|---|---|---|
| Auth - Login | Validates authentication logic with valid/invalid username and password | Passing |
| Auth - Signup | Validates user creation, duplicate checks, and JWT generation | Passing |
| Components Fetch | Ensures components can be queried and filtered by category/search | Passing |
| Saved Builds | Validates creation, deletion, and fetching of user builds | Passing |
| Favourites API | Checks add, remove, and get endpoints for user favourite components | Passing |
| Session Validation | Verifies OptionalAuth middleware and token expiration logic | Passing |
